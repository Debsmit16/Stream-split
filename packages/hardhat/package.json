{
  "name": "@stream-split/hardhat",
  "version": "1.0.0",
  "description": "StreamSplit smart contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network alfajores",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network celosepolia",
    "verify": "hardhat verify --network alfajores"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@typechain/ethers-v6": "^0.5.0",
    "@typechain/hardhat": "^9.0.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.19.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  }
}
